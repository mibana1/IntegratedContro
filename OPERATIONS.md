# IntegratedContro 운영 절차

일반 장비는 가상 모델이며 Hiperwall 편집은 설정한 실제 Controller로 명령을 보낸다. 서비스·자동 실행 등록은 별도다.
Windows 11 25H2 Pro·Enterprise / x64가 지원 대상이며 실제 확인 환경은 검증 기록을 따른다.

## 카메라 등록 성공 확인 (2026-09-15)

새 카메라를 저장하면 하단에 “카메라 등록 성공: '카메라 이름' · 영상 경로 준비 중”이 표시된다.
MediaMTX 설정이 적용되면 경로 준비 완료로 갱신되며, 비활성 카메라는 비활성 저장 상태가 표시된다.
기존 카메라 변경은 “카메라 수정 성공”으로 표시한다. 실제 영상은 재생 버튼으로 확인한다.

## 카메라 등록 오류 확인 (2026-09-15)

카메라 등록·수정이 실패하면 앱 맨 아래 메시지 영역에서 원인을 확인한다.
설정 누락과 RTSP 주소 오류는 즉시 표시하며, 저장 후 MediaMTX 경로 준비가 실패한 경우에는 카메라 이름과 인증·권한 등의 원인을 표시한다.
응답 시간이 초과되면 등록 여부를 목록에서 확인한 후 다시 시도한다. 등록 저장 여부와 실제 영상 입력 준비 여부는 별개다.
## 로그인창 닫기와 카메라 자동 갱신 (2026-09-15)

- 로그인창에서 X·취소·Esc를 누르면 앱 전체를 종료한다. 접속 대기 중과 로그아웃 후 열린 창에도 적용한다.
  접속 성공 시에는 로그인창만 닫히고 메인 화면을 유지한다. 별도 서버와 MediaMTX는 계속 실행된다.
- 카메라 자동 갱신은 버튼과 입력을 유지한다. 조회 중 누른 명령도 처리하며, 실제 저장/삭제 등 명시적 작업 중에는 중복 조작을 차단한다.
  목록은 바뀐 항목만 갱신하고 선택과 재생을 유지한다.
## 최신 앱 바로가기와 빌드 보관 (2026-09-15)

작업 공간 C:\Users\노주형\Desktop\IntegratedContro의 **IntegratedContro.lnk**가 최신 App을 실행한다.
필요한 DLL·런타임·영상 엔진은 해당 빌드 폴더에 두며 작업 공간에는 복사하지 않는다.

- 검증 후 scripts/publish.ps1을 실행하면 새 Build 폴더에 App/ControlHost를 함께 생성하고 바로가기를 갱신한다.
- 완성된 배포 빌드는 최신 3개만 보관한다. 이전 Current·App·ControlHost·Lighting 등 중복/과거 배포 폴더도 정리 대상이다.
- 실행 중인 빌드, 잠긴 파일, 운영 DB/보호 파일 또는 재분석 지점이 있는 폴더는 정리를 보류한다.
  사용 중인 이전 빌드를 정상 종료한 뒤 다음 배포/동기화에서 다시 정리한다.
- 바로가기 갱신은 실행 중인 앱을 종료하지 않는다. 현재 앱을 닫고 바로가기로 다시 열면 최신 버전이 실행된다.
- 서버 실행 파일을 교체하려면 진행 작업을 확인하고 기존 서버를 정상 종료한 뒤
  scripts/start-lighting-host.ps1 -DataPath <기존 데이터 폴더>를 실행한다. 이 스크립트는 최신 완성된 ControlHost를 선택한다.
- 최신 경로·정리 결과는 artifacts/publish/publish-result.json과 shortcut-result.json에 저장한다.
  이전 절에 적힌 날짜별 배포 경로는 당시 기록이며 최신 3개 정리 후 없을 수 있다.

이미 완성된 최신 빌드의 바로가기/보관 규칙만 다시 적용하려면:

~~~powershell
.\scripts\sync-published-app.ps1 -BuildPath '완성된 최신 빌드의 전체 경로'
# 변경 없이 정리 예정 목록 확인
.\scripts\sync-published-app.ps1 -BuildPath '완성된 최신 빌드의 전체 경로' -Preview
~~~

기존 data 폴더, 카메라용 MediaMTX, 소스와 검증 도구는 배포 정리 대상이 아니다.
실패한 빌드의 .publish-in-progress 표시가 있으면 최신 실행본으로 선택하지 않는다.

## 카메라 등록 입력 수정 (2026-09-15)

자동 목록 갱신 중 등록·MediaMTX 입력창이 비활성화되어 입력이 끊기던 문제를 수정했다.
수정된 App은 artifacts/publish/CameraInputFix-*의 App 폴더에 생성하며,
정확한 경로는 artifacts/publish/camera-input-fix-path.txt에 기록한다.
운영 앱을 정상 종료한 뒤 새 App 폴더의 IntegratedContro.App.exe를 실행한다.
이 수정은 앱만 교체하면 적용되므로 기존 MediaValidation-20260914-162930 ControlHost와
MediaMTX를 계속 실행해도 된다. 새 setup이나 서버 데이터 변경은 필요하지 않다.

## 카메라 · MediaMTX · 영상 · 프리뷰 (2026-09-14)

앱과 ControlHost를 같은 빌드로 사용한다. 기존 호스트 데이터 폴더를 유지하며 setup을 다시 실행하지 않는다.
이번 검증 배포본은 artifacts/publish/MediaValidation-*에 따로 생성했다. 현재 운영 프로세스·Current 배포본은 교체하지 않았다.

1. ControlHost PC에서 별도 MediaMTX 1.21.0을 준비한다. [설정 예제](config/mediamtx.example.yml)를
   운영용 폴더에 복사하고 API·HLS의 서로 다른 전용 사용자/비밀번호를 설정한다.
   예제는 동일 PC의 loopback 통신, HLS mpegts, 메모리 세그먼트, 사용 종료 10초 후 muxer 해제다.
   MediaMTX 프로세스의 시작·종료·서비스 등록은 운영에서 관리한다.
   다른 PC에 둘 때는 주소·IP 접근 범위·TLS를 해당 환경에 맞게 설정하고, 서버 주소는 ControlHost에서 접근 가능해야 한다.
2. 관리자 로그인 → **사용 시작 → 카메라 · 영상 → MediaMTX**에서 API/HLS 주소와 각 계정을 저장한다.
   설정 저장은 연결·영상 수신 완료를 뜻하지 않는다. 기존 대상·사용자의 비밀번호를 비우면 보호 값을 유지한다.
   대상 주소나 사용자 이름 변경은 등록 카메라와 경로 정리 큐가 비어 있어야 한다.
3. **등록 · 수정 → 새 카메라**에서 이름·위치·RTSP 주소, 필요한 RTSP 계정을 입력하고 저장한다.
   RTSP 주소 전체는 보호 입력이며 기존 카메라 수정에서 주소와 계정을 모두 비우면 유지한다.
   **선택 불러오기**로 현재 버전의 카메라를 편집한다. 등록·수정은 관리자+사용권이 필요하다.
4. 저장된 등록과 MediaMTX 경로 준비 상태를 따로 확인한다. 준비 완료 후 **실제 입력 상태**로 RTSP 입력의 ready를 조회한다.
   입력이 없는 경로를 영상 수신 성공으로 표시하지 않는다.
   **재생 · 다시 연결**은 로그인한 조회자/운영자도 사용할 수 있으며 HLS 지연이 포함된다.
5. 초기 영상은 음소거다. **영상 음소거**는 해당 앱의 재생만 제어한다.
   확인한 코덱은 H.264/AAC이며 MediaMTX가 지원하지 않는 카메라 코덱을 자동 변환하지 않는다.
   영상이 15초 동안 진행되지 않거나 연결이 끝나면 최대 5회 재연결한다. 실패 종료 후에는 상태를 확인하고 다시 재생한다.
6. **재생 중지**, 카메라 변경, 비활성화/삭제 관측, 탭 이탈, 최소화, 로그아웃, 앱 종료 시
   요청 취소 → 로컬 중계 종료 → 네이티브 플레이어/엔진 해제 순서로 정리한다.
   화면 복귀 후 재생은 사용자가 누른다. 영상 조회가 실패해도 사용권 반납·로그아웃은 계속 가능하다.
7. **활성화** 해제 후 저장하면 MediaMTX 경로를 제거하고 등록은 유지한다.
   **재동기화**는 저장된 설정으로 경로를 다시 맞춘다. 호스트 시작 시 재동기화하며 정상 경로도 30초마다 확인한다.
8. **삭제**는 경로 제거 성공 후 등록을 지운다. 실패 시 등록과 삭제 실패 상태를 유지하며 명시적인 재시도를 기다린다.
   **강제 삭제** 확인을 승인하면 등록만 즉시 지우고 원 서버 설정·정확한 경로·원 요청자를 영속 정리 큐에 남긴다.
   큐는 호스트 시작 시 즉시, 실패 후 30초마다 재시도한다. **강제 삭제 경로 정리 → 정리 큐 다시 시도**로 당길 수 있다.
   같은 카메라 ID를 다시 등록해도 새 경로를 사용하므로 이전 정리 작업이 새 경로를 삭제하지 않는다.
   사용권 반납·로그아웃·앱 종료는 호스트가 접수한 경로 정리를 취소하지 않는다.

Hiperwall 매핑은 현재 Contents에서 고른 UUID 또는 유일한 전체 이름을 저장한다.
카메라 등록은 Hiperwall Source를 생성하거나 콘텐츠를 자동 표시하지 않는다. RTSP 영상과 Controller 이미지 프리뷰는 별도 기능이다.

Hiperwall 탭은 현재 보이는 인스턴스의 JPEG/PNG를 요청하며 같은 콘텐츠의 이미지를 공유한다.
스트림 등 일반 콘텐츠는 3초, 이미지 콘텐츠는 60초 간격이다. 실패하면 이전 이미지와 편집을 유지하고
노란 테두리·느낌표를 표시한다. 충분히 큰 사각형에는 실패 문구도 표시한다.
이미지는 디스크에 저장하지 않으며 탭 이탈·최소화·로그아웃에서 요청과 캐시를 비운다.

보호·자원 한도:

- RTSP 주소 전체와 API/HLS 비밀번호는 지정 호스트 데이터 폴더의 media-*.dpapi에 현재 Windows 사용자 범위로 보호한다.
  DB·일반 조회에는 보호 참조만 둔다. 호스트 데이터 보관에는 DB와 DPAPI 파일을 함께 포함한다.
- 앱은 인증서 지문을 확인한 HTTPS ControlHost를 통해 영상/프리뷰를 조회한다.
  네이티브 엔진에는 재생마다 생성한 loopback 주소와 일회성 Basic 자격 증명만 메모리로 전달한다.
  호스트 인증 토큰·RTSP 계정·HLS 계정을 재생 URL에 넣지 않는다.
- 호스트의 HLS 동시 읽기는 4개, 프리뷰는 2개다. HLS는 파일당 32MiB, 목록은 128KiB까지다.
  외부 주소·상위 경로·임의 쿼리가 포함된 재생 목록은 거부한다.
- 프리뷰는 압축 이미지 4MiB, 한 변 4096px/400만 픽셀, 디코딩 캐시 64개/24MiB까지다.
  표시용 이미지는 너비 최대 1024px로 축소한다.
- 저장 aggregate에 기본값이 있는 필드를 추가했으며 기존 schema 1 DB를 유지한다.
  카메라 데이터를 저장한 뒤 구형 호스트로 덮어쓰면 새 필드를 보존하지 못하므로 같은 버전의 앱/호스트 쌍을 사용한다.

Hiperwall 기본 조회·추가·변경·닫기의 실제 연동은 사용자가 확인했다.
이번 변경은 생성 영상과 격리된 로컬 MediaMTX로 검증했으며 현장 카메라와 Controller 이미지 프리뷰는 현장 확인이 남아 있다.
[검증 기록과 재현 명령](docs/MEDIA_VALIDATION.md)을 참고한다.

## Hiperwall LIVE 편집 · 2026-09-14

앱과 호스트를 같은 빌드로 함께 업데이트한다. 구형 호스트에서는 편집 버튼이 활성화되지 않는다.
배포 결과 파일 `artifacts/publish/publish-result.json`의 `staging`이 완성된 새 배포 경로이며,
앱은 작업 공간의 IntegratedContro.lnk로 실행하고, 호스트를 업데이트할 때는 기존 호스트를 정상 종료한 뒤 staging의 ControlHost를 사용한다.
호스트는 기존 데이터 경로로 `run --data <기존 데이터 폴더>`를 사용한다. setup으로 재초기화하지 않는다.

1. 관리자 또는 전체 장비 제어 권한이 있는 운영자로 로그인하고 **사용 시작**을 누른다.
   복구 필요 상태라면 **복구 · 진단 → 진행 작업 확인 → 관리자 복구 인계 승인** 후 사용 시작한다.
2. **Hiperwall 편집 → 목록 새로 고침**으로 연결과 현재 인스턴스를 확인한다.
3. Contents를 선택하고 오른쪽 **배치할 Zone**을 지정한 뒤 **선택 콘텐츠 추가**를 누른다.
   마우스로 Contents 카드를 Zone에 끌어 놓을 수도 있다. 원본 크기는 자동 축소하지 않으며,
   크기 미제공 콘텐츠는 너비·높이를 직접 입력한다. 같은 이름이 여러 개면 UUID로 구분한다.
4. 열린 콘텐츠를 선택해 마우스나 한 손가락으로 드래그해 이동한다. 오른쪽 아래 노란 손잡이를 끌면 크기가 조절된다.
   Zone 정보가 없는 인스턴스는 오른쪽에서 이동할 Zone을 먼저 선택한다. 이미 선택한 유효 Zone은 유지된다.
   놓을 때 최종 위치가 LIVE에 한 번 반영된다. Esc/선택 변경/캡처 상실/사용권 상실/두 번째 터치는 진행 중인 드래그를 취소한다.
   두 손가락 핀치 크기 조절은 제공하지 않는다. 터치에서 콘텐츠 추가는 Contents와 Zone 선택 후 추가 버튼을 사용한다.
   좌표·크기 입력은 **위치·크기 적용**을 누르며, Y는 아래 방향의 절대 픽셀 중심 좌표다.
5. 캔버스 아래의 **Zone 버튼**을 클릭/탭하면 해당 Zone으로 화면이 이동한다.
   열린 소스를 버튼에 끌어 놓으면 **이동 전 크기를 유지**하며 그 Zone 중앙으로 이동한다. 마우스와 한 손가락 드래그를 지원한다.
   Contents를 Zone 버튼에 놓으면 원본 크기로 새로 추가한다. Zone보다 큰 소스도 축소하지 않는다.
   **Zone 중앙 이동** 버튼으로도 선택한 Zone 중앙으로 현재 크기를 유지해 옮길 수 있다.
   인스턴스의 Zone 식별자가 응답에 없으면 사용자가 대상 Zone을 직접 선택해야 한다.
   비율 유지와 Zone 격자 맞춤은 드래그 편집에 적용하며 Zone 경계 밖으로 이동할 수 있다.
6. 소리 정보를 제공하는 인스턴스에서 슬라이더를 조절하고 **음량 적용**을 누른다.
   음량 변경은 음소거 상태를 유지한다. **선택 음소거/해제**, **전체 음소거/해제**는 별도 명령이다.
7. **선택 닫기**는 그 Instance ID만 닫는다. **전체 닫기…**는 확인한 목록을 개별 종료한다.
   다른 앱이 연 인스턴스도 확인 목록에 있으면 포함한다. 목록이 바뀌면 재확인을 요구한다.
8. **전송 기록** 탭에서 요청자·대상·응답 확인/거부/결과 확인 필요 건수를 확인한다.
   **전송 기록 확인**은 접수 결과를 조회하며 명령을 다시 보내지 않는다.
   현재 사용권자는 **선택 기록의 남은 전송 취소**로 미전송 부분만 취소할 수 있다.
   이미 전송한 명령의 정지·원상복구는 아니다. 새로 고친 목록과 실제 표시·소리를 확인한다.

앱 종료/사용 종료는 접수된 편집을 취소하지 않는다. 호스트 재시작 시 진행 중 편집은 자동 재전송하지 않는다.
연결 테스트는 계속 읽기만 수행하며, 편집 명령의 응답 확인은 물리 표시·음향 검증과 구분한다.
이번 실행 검증은 가짜 Controller에 한정하며 실제 현장 Controller에는 검증용 쓰기를 보내지 않았다.

## Hiperwall 저장 배치·수동 표시·복구

App과 ControlHost를 이번 배포본으로 함께 업데이트하고 호스트는 기존 데이터 폴더로 실행한다.
운영 중인 호스트는 자동으로 종료하거나 교체하지 않는다.

1. **Hiperwall 편집 → LIVE 편집 → 목록 새로 고침**으로 Contents와 Zone을 읽는다.
2. **저장 배치 · 표시 · 복구**에서 **새 배치**를 선택하고 이름을 입력한다.
   콘텐츠·Zone·중심 X/Y·너비·높이를 입력해 **초안에 추가**한다.
   항목을 선택해 수정·제거하거나 **현재 조회한 LIVE를 초안으로 복사**할 수 있다.
3. 표시 시간을 기본 15초 / 지정 시간(1~86,400초) / 계속 표시 중 선택하고 **배치 저장**을 누른다.
   저장은 현재 영상벽을 변경하지 않는다. 저장 배치를 선택한 뒤 **초안으로 불러오기**로 다시 편집한다.
4. **저장 배치 표시**는 선택한 저장 버전을 표시한다. 화면에서 편집 중인 미저장 값은 사용하지 않는다.
   **초안 15초 테스트**는 현재 초안을 임시 표시하며 저장 배치는 바꾸지 않는다.
   개별 인스턴스·요청자·종료 시각을 호스트에 먼저 저장하므로 UI를 닫아도 종료 일정은 유지된다.
5. 오른쪽 **표시 · 정리 작업**과 **작업 · 교대**에서 원 요청자·PC·세션·진행 및 정리 결과를 확인한다.
   다음 사용권자도 **선택 표시 종료 · 정리 재시도**로 미전송 부분을 중단하고 열린 표시를 닫을 수 있다.
   배치를 삭제해도 이미 접수된 표시는 유지된다.
6. 호스트가 중단되면 동일 데이터 폴더로 다시 실행한다. 미확정 open은 재전송하지 않고 목록과 대조한다.
   만료 테스트·이전 종료 작업은 이어 정리한다. 정상 호스트 종료에서는 이 앱이 소유한 LIVE 추가·배치 표시만 정리한다.
   완료하지 못한 정리는 보존하며 다른 앱이 연 인스턴스는 유지한다.
7. **확인 필요** 작업은 원 Controller 주소·인증·권한·콘텐츠를 확인한 뒤 정리를 재시도한다.
   같은 ID가 다른 콘텐츠로 바뀌면 자동 닫기를 차단한다. 재시도도 실패하면 현재 Controller 목록에서 확인한다.

검증은 격리된 테스트 Controller와 호스트/WPF로 수행했다. 현장 Controller의 새 배치·종료 정리 흐름은 별도 확인이 필요하다.

## Hiperwall 연결 설정·조회

최신 앱과 호스트를 함께 업데이트한다. 기존 데이터 폴더로 run하며 setup을 다시 실행하지 않는다.
Hiperwall 기능은 가상 조명과 독립적이다. 실패 시 가상 장비나 다른 Controller로 자동 대체하지 않는다.

1. 관리자 앱 계정으로 로그인하고 **사용 시작**을 누른다.
2. **관리자 설정 → Hiperwall 연결**에서 **현재 적용 설정 불러오기**를 누른다.
3. 연결 이름, 실제 주소·포트, None/Token 인증, Token 사용자, 새 토큰, 전체 제한시간을 입력한다.
   토큰은 앱의 비밀번호 입력칸에만 넣는다. 저장된 값은 반환되지 않는다.
   입력칸을 비우면 같은 대상·사용자의 기존 토큰을 유지한다. 대상이나 사용자가 바뀌면 새 토큰을 입력한다.
4. **연결 설정 저장·적용**을 누른 뒤 표시된 설정 버전을 확인한다.
   **적용 설정 연결 테스트**는 저장된 설정으로 /hello, list, walls, list/open 읽기만 수행한다.
   편집 중인 미저장 내용은 테스트에 적용되지 않는다. 테스트는 관리자에게 허용되며 사용권 없이도 가능하다.
5. **Hiperwall 편집**에서 **목록 새로 고침**을 누른다. 모든 활성 인증 앱 계정은 조회할 수 있다.
   Wall/Zone/Contents/열린 콘텐츠의 성공·빈 목록·미지원·오류와 마지막 성공 시각을 각각 확인한다.
   선택 항목은 응답 식별자와 실제 수신 필드만 상세에 표시한다.
6. 조회 실패 시 이전 목록은 비워진다. “연결 성공”은 읽기 요청 성공이며 영상벽의 쓰기 권한·물리 상태 검증을 뜻하지 않는다.
   Wall에 “상세 명세 미확인”이 보이면 현재 버전의 API 매뉴얼을 확보한다. Zone을 Wall로 대신 표시하지 않는다.
7. **복구 · 진단**에서 “Hiperwall 연결 설정 변경”, “Hiperwall 연결 테스트” 감사 기록을 확인한다.
   일반 새로 고침은 반복 감사 이벤트를 만들지 않는다.

현장 전달값은 URL http://127.0.0.1:8000, Token, 사용자 3, 3000ms였으나 실제 응답은 확인되지 않았다.
앱 기본값으로 저장하지 않았다. 127.0.0.1은 **ControlHost PC 기준**이다.
HTTP는 호스트→Controller 구간을 암호화하지 않는다. HTTPS를 사용하려면 실제 TLS 주소·유효 인증서를 확인한다.
Crypto 인증은 미지원이다. 저장 배치·수동 표시·영상 프리뷰는 위의 최신 운영 절을 따른다.

| 표시 | 확인할 사항 |
|---|---|
| 설정되지 않음 / 연결 확인 전 | 관리자 설정 저장 여부 / 저장 후 명시적 테스트 여부 |
| 인증 실패 | Controller의 활성 인증 방식, 사용자·토큰, 조회 권한. 토큰을 로그에 복사하지 않음 |
| 응답 시간 초과 | 호스트 기준 주소·네트워크·Controller 부하와 전체 조회 제한시간 |
| 연결 실패 | HiperInterface 가동·주소·포트·TLS, 보호 저장 파일·호스트 실행 계정. 다른 폴더로 DB를 만들지 않음 |
| 미지원 API/형식 | 해당 Controller 버전의 API 명세, list/walls 응답 구조, Wall/Zone 모드 |
| 설정 변경 / 버전 충돌 | 적용 설정을 다시 불러온 뒤 편집·새로 고침 |
| 호스트가 조회 미지원 | 앱과 ControlHost가 같은 기능을 지원하는 최신 배포본인지 확인 |

데이터 백업에는 기존 SQLite·host.json·인증서와 같은 폴더의 hiperwall-*.dpapi를 포함한다.
Windows 실행 계정이 바뀌면 DPAPI 접근을 재검증하고 필요 시 앱에서 토큰을 다시 입력한다.
호스트를 재시작하면 설정은 복구하지만 이전 목록은 불러오지 않으며 다시 조회해야 한다.
프로토콜 근거와 미검증 범위는 [Hiperwall 프로토콜 기록](docs/HIPERWALL_PROTOCOL.md),
실행한 검증은 [Hiperwall 검증 기록](docs/HIPERWALL_VALIDATION.md)을 따른다.

### 이전 조회 화면 구성 참고 · 2026-09-10

- 왼쪽 **Contents**에서 이름·경로·UUID·유형을 검색한다. 유형 필터와 이름의 경로별 분류를 제공한다.
  “검색 0 / 전체 N”은 검색 결과 없음이며 Controller의 빈 목록과 다르다.
- 가운데 **Wall · Zone 캔버스**는 Controller가 반환한 Zone과 열린 콘텐츠의 좌표 영역이다.
  녹색은 Zone, 파란색은 열린 인스턴스, 노란 테두리는 선택 항목이다. 실시간 영상 프리뷰는 아니다.
- 휠 또는 +/−로 확대·축소하고 **전체 맞춤**, **선택 맞춤**으로 보기 범위를 조절한다.
  오른쪽/가운데 버튼을 누른 채 드래그하면 화면만 이동한다. 캔버스에 키보드 초점이 있으면 F는 전체 맞춤,
  +/−는 확대·축소, Esc는 선택 해제다. 좌표나 크기를 Controller에 쓰지 않는다.
- 캔버스 아래 **열린 콘텐츠 / Zone / Wall**에서 목록별 조회 시각·오류를 확인하고 선택한다.
  Contents의 UUID와 열린 콘텐츠의 인스턴스 ID는 서로 다른 식별자다.
- 오른쪽 **선택 항목 상세**에서 위치·크기와 실제 응답 필드를 확인·복사한다.
  좌표 미제공·회전 규약 미확인·표시 범위 초과 항목은 목록에서 선택하면 캔버스 제외 사유가 보인다.
  이름이나 가장 가까운 Zone으로 소속을 추정하지 않는다.
- 열린 콘텐츠 미지원은 open 필터 또는 Instance 응답 형식이 지원되지 않는 경우다.
  콘텐츠 목록·Zone 목록이 별도로 성공했으면 해당 목록의 성공 상태는 유지한다.
  원문 응답이 필요하면 현장에서 비밀값을 제거한 예시와 버전별 매뉴얼을 확보한다.

## 접속·감사 기록·일괄 조명 업데이트 · 2026-09-09

- 앱을 시작하면 **호스트 접속 팝업**이 열린다. 호스트 HTTPS 주소, SHA-256 인증서 지문, 앱 계정, 비밀번호를 입력한다.
  기존에 저장한 주소·지문은 불러오며 비밀번호·토큰은 저장하지 않는다. 접속 시도 후 비밀번호 입력칸을 비운다.
  성공하면 팝업이 닫히고 운영 화면을 표시한다. 취소했다면 우측 위 **접속**으로 다시 연다.
- **로그아웃**은 우측 위 작은 버튼으로 이동했다. 로그아웃 후 접속 팝업이 다시 열린다.
  접수 작업은 기존 규칙대로 호스트에서 계속 처리된다.
- **관리자 설정** 탭은 호스트에 등록된 **관리자 권한 계정**으로 로그인했을 때만 표시한다.
  운영자·조회자·미로그인 상태에서는 숨긴다. 설정 변경에는 기존처럼 관리자 권한과 현재 사용권이 모두 필요하다.
  여기서 호스트 계정은 앱의 Administrator 권한을 뜻하며 Windows 실행 계정이나 PC 위치로 판정하지 않는다.
- **복구 · 진단**의 감사 기록은 이 PC의 현지 시각, 사용자, 한국어 동작 이름, 대상·PC·동작 설명으로 표시한다.
  **선택 기록의 원본 상세**를 펼치면 원본 이벤트 코드와 ID를 확인할 수 있다.
  새 기록은 표시 이름·설명을 발생 시점에 함께 저장한다. 이전 기록에 이름이 없으면 현재 설정을 참고하며,
  작업 대상은 저장된 snapshot을 사용한다. 원본 시각·이벤트·ID·내용은 보존한다.

### 조명 전체·그룹 ON/OFF

1. 로그인 후 **사용 시작**으로 사용권을 얻는다.
2. 조명 모아보기 우측 위 **전체 ON / 전체 OFF**는 등록된 조명 전체를 대상으로 한다.
   각 그룹의 **그룹 ON / 그룹 OFF**는 그 그룹의 조명만 대상으로 하며 미분류에도 사용할 수 있다.
3. 모든 대상의 역할·권한·확인 상태가 준비되어 있어야 한다.
   하나라도 상태 미확인·권한 없음·비활성·작업 처리 중·시나리오 예약·불확실 상태이면 이유를 표시하고 **전체 접수를 거부**한다.
   일부 장비를 자동으로 제외하지 않는다. 시나리오 예약은 기존 명시적 중단·대조 흐름을 먼저 따른다.
4. 한 번 누르면 현재 대상 PC ID·장비 ID·역할/설정 버전·전원 상태와 ON/OFF 요청값을 고정해 일괄 작업 하나로 접수한다.
   이미 ON인 조명에 전체 ON을 눌러도 OFF로 뒤집히지 않는다.
   그룹 이름·구성원 변경은 이미 접수된 작업의 대상을 바꾸지 않는다.
5. 작업은 표시된 대상 순서로 실행한다. 각 전송 전에 권한·설정·최신 가상 상태 조건을 재검증한다.
   실패·상태 변경·결과 불확실이면 남은 단계는 멈추며 이미 전송한 결과는 보존한다.
   일괄 접수는 1~1000개, 만료는 접수 후 5분이다. 긴 작업은 만료 이후 단계 전송을 차단한다.
6. **작업 · 교대**에서 일괄 조명 작업의 대상·단계·결과를 확인하고 남은 단계를 취소할 수 있다.
   사용 종료·로그아웃·UI 종료는 작업을 취소하지 않는다. 다음 사용권자는 대상 전체의 권한이 있을 때 선택 취소할 수 있다.
   호스트 재시작 시 전송을 시작한 일괄 작업은 자동 재개하지 않는다.
   아직 전송을 시작하지 않은 접수 작업만 재검증 후 실행할 수 있다.
7. 편집 중·사용권 없음에는 일괄 버튼을 사용하지 않는다. 접수 응답이 불확실하면 같은 요청 ID로만 접수 여부를 재확인한다.

일괄 명령과 새 감사 형식을 사용하려면 **최신 빌드의 앱과 호스트를 함께 업데이트**한다.
새 LightBatch 작업이 저장된 데이터는 구형 호스트가 해석할 수 없으므로 예전 EXE로 되돌려 실행하지 않는다.
기존 DB는 최신 호스트가 그대로 읽으며 setup을 다시 하지 않는다.

## 실행 및 조명 그룹·드래그 편집

최신 배포 위치는 **artifacts/publish/publish-result.json의 staging**이며 App/ControlHost가 함께 들어 있다.
그룹·일괄 조명·감사 기록을 위해 두 실행 파일을 함께 업데이트한다. App-role-fix와 Lighting 폴더는 이전 배포본이다.
publish.ps1은 새 Build 폴더를 만들고 작업 공간의 앱 바로가기를 갱신한다. 실행 중 프로세스는 자동으로 교체하거나 종료하지 않는다.

1. 작업 탭에서 진행 상황을 확인한 뒤 기존 앱을 정상 종료하고, 기존 호스트 콘솔에서 Ctrl+C로 종료한다.
   호스트 재시작은 진행 중 시나리오를 자동 재개하지 않으므로 먼저 작업 결과를 확인한다.
2. 저장소 폴더의 PowerShell에서 아래 명령을 실행한다.
   데이터 경로에는 **최초 설정에서 사용한 기존 폴더**를 입력한다. setup을 다시 하지 않는다.

   ```powershell
   $siteData = Read-Host '기존 호스트 데이터 폴더의 전체 경로를 입력하세요'
   & '.\scripts\start-lighting-host.ps1' -DataPath $siteData
   ```

   Read-Host가 입력을 기다릴 때는 폴더 경로만 입력한다. 다음 실행 명령을 답변으로 입력하지 않는다.
   이미 경로를 알고 있다면 `$siteData = '기존 데이터 폴더 전체 경로'`로 직접 대입해도 된다.
   EXE 직접 실행은 PowerShell 스크립트 실행 정책을 변경할 필요가 없다.
   start-lighting-host.ps1은 같은 run 명령의 보조 스크립트이며 실행 정책을 자동 변경하지 않는다.
3. 작업 공간의 **IntegratedContro.lnk**를 실행한다.
   시작 팝업에서 기존 호스트 주소·지문·계정으로 로그인하고 사용 시작을 누른다.
4. **장비 제어 → 조명 모아보기**에서 조명을 확인한다.
   작은 카드 위쪽에는 조명 이름·PC 이름, 가운데에는 전구와 ON/OFF를 표시한다.
   상태 미확인은 **상태 확인**, 역할 미배정은 **상세 설정**에서 처리한다.
5. 일반 화면에서 카드를 한 번 클릭하거나 터치하면 전원 명령을 접수한다.
   처리 중·사용권 없음·권한 없음·불확실 상태·시나리오 예약 시에는 기존 제약을 유지한다.
6. 관리자+사용권으로 **그룹 · 순서 편집**을 누른다.
   이름을 입력하고 **그룹 추가**를 누르면 빈 그룹이 생긴다. 그룹 이름 입력칸에서 이름을 변경할 수 있다.
7. 카드를 마우스로 누른 채 끌거나 손가락으로 터치한 채 끌어 순서를 바꾼다.
   다른 카드의 왼쪽/오른쪽에 놓으면 앞/뒤로 들어가고, 다른 그룹의 빈 영역에 놓으면 그 그룹 끝으로 이동한다.
   파란 선/테두리가 놓을 위치를 표시한다. 화면 위아래 가장자리에서는 자동 스크롤한다.
   편집 중에는 전원 명령을 접수하지 않는다. Esc, 창 비활성화, 사용권 상실, 영역 밖 놓기는 해당 드래그를 취소한다.
8. **그룹 삭제**는 조명을 **미분류**로 옮긴다. 장비·역할·작업은 삭제하지 않는다.
   **저장**은 그룹과 조명 순서를 함께 호스트에 반영하고 **취소**는 저장 전 초안을 버린다.
   저장 내용은 다른 앱과 재접속·호스트 재시작 후에도 유지된다.
   다른 저장이나 조명 목록 변경과 충돌하면 편집을 취소하고 다시 시작한다.

그룹은 최대 100개, 이름은 1~50자이며 중복 이름을 허용하지 않는다. 한 조명은 한 그룹에 속한다.
기존 조명은 저장된 순서를 유지한 채 미분류에 표시된다. 그룹 편집 자체는 제어 권한을 변경하거나 접수된 명령의 고정 대상을 바꾸지 않는다.
기존 조광 조명도 전원 카드로 표시한다. 밝기·다른 장비 기능은 **장비 목록 · 상세 조작**에서 사용한다.
물리 터치 패널과 실제 두 PC 검증은 별도로 필요하다.

## 최초 호스트 설정

빌드된 호스트 EXE 또는 publish 폴더 전체를 사용할 수 있다. SDK 없는 PC에는 self-contained publish 폴더를 복사한다.
호스트 EXE 하나만 복사하지 않는다. SQLite 네이티브 DLL 등 같은 폴더의 파일이 필요하다.

호스트 PC의 로컬 고정 디스크에서 데이터를 보관할 폴더를 선택한다.
실행 계정이 읽고 쓸 수 있고 접근 권한을 신뢰할 수 있는 폴더를 사용한다. UNC·네트워크 드라이브·재분석 경로는 받지 않는다.
실제 운영 경로를 코드에서 정하거나 실패 시 다른 폴더로 바꾸지 않는다.

다음 예시의 변수는 실행 시 입력한다. 비밀번호는 명령행에 전달하지 않는다.

```powershell
$latestBuild = (Get-Content '.\artifacts\publish\publish-result.json' -Raw | ConvertFrom-Json).staging
$hostExe = Join-Path $latestBuild 'ControlHost\IntegratedContro.ControlHost.exe'
$siteData = Read-Host '호스트 로컬 데이터 폴더의 절대 경로'
$siteName = Read-Host '현장 이름'
$adminName = Read-Host '최초 관리자 앱 계정'
$bindIp = Read-Host '호스트 수신 IP (로컬만 127.0.0.1, LAN 수신은 지정 IP 또는 0.0.0.0)'
$listenPort = Read-Host 'HTTPS 포트 (1024~65535)'
& $hostExe setup --data $siteData --site $siteName --admin $adminName --bind $bindIp --port $listenPort --heartbeat-timeout 15
# 비밀번호 12~256자와 확인 값을 숨김 입력한다.
& $hostExe run --data $siteData
```

setup은 로컬 실행 권한을 가진 사람만 수행하는 독립된 절차다.
기존 DB가 있으면 중복 초기화를 거부한다. run은 지정 폴더·DB·설정·인증서 중 하나라도 열 수 없으면 종료한다.
초기화 중 실패한 폴더도 run이 새 현장으로 대체하지 않는다. 오류 원인을 확인한 뒤 운영 담당자가 명시적으로 정리해야 한다.
호스트 PC의 수신 포트와 네트워크/방화벽 정책은 현장 담당자가 허용 여부를 확인한다. 구현·검증 과정에서 방화벽 설정은 바꾸지 않았다.

데이터 구성:

- `control.sqlite`, WAL/SHM: 현장 ID, 계정 해시, 설정, 사용권, snapshot, 작업·감사 기록, 가상 상태
- `host.json`: 수신 IP·포트·생존 확인 제한·공개 인증서 지문
- `host-certificate.dpapi`: 현재 Windows 계정으로 보호된 인증서/개인 키
- `host-certificate.cer`: 공개 인증서, `host.lock`: 단일 호스트 파일 소유권
- 호스트 개인 키는 실행 시 Windows Schannel 호환을 위해 보호된 사용자 키 저장소에 로드된다.
  영구 키 유지 플래그는 사용하지 않는다. 실행 계정/프로필과 보호 저장 접근이 필요하다.

정상 실행에서는 콘솔 입력을 기다리지 않는다. 서비스 전환은 이후 진입점과 운영 정책을 추가하는 작업이다.

## 앱 연결·계정·장비 설정

1. `IntegratedContro.App.exe`를 연다. 호스트를 시작하거나 같은 PC를 제어 대상으로 자동 선택하지 않는다.
2. `https://실제호스트주소:포트`, 최초 설정/호스트 출력의 **SHA-256 인증서 지문**, 앱 계정과 비밀번호로 접속한다.
   지문은 호스트에서 확인한 값을 사용한다. 인증서 검사를 끄는 옵션은 없다.
3. 관리자는 **사용 시작**을 누르고 관리자 설정에서 운영자·조회 계정을 만든다.
   공유 설정과 계정 변경에는 관리자 권한과 현재 사용권을 모두 요구한다.
4. 장비 이름, 명시적 PC ID·장비 ID, 실제 대상 PC 이름, 현장 연결 ID, 가상 모델을 등록한다.
   최초 PC ID 입력값은 해당 앱 설치의 안정된 ID이고 PC 이름은 현재 실제 PC 이름이다.
   다른 가상 대상 PC를 표현하려면 그 대상의 ID·이름을 명시한다. 현재 단계는 인증된 원격 DeviceAgent 식별 검증을 제공하지 않는다.
5. 장비 제어 탭에서 장비를 선택하고, 같은 화면 하단의 **선택 장비 역할 배정 · 관리자**에서
   대상 장비·PC 이름·장비 ID를 확인한다. 역할 ID(예: `room.light`)를 입력해 **선택 장비에 역할 배정**을 누른다.
   관리자 설정의 **배정할 등록 장비** 목록에서도 대상을 직접 선택할 수 있으며 장비 제어의 선택과 연동된다.
   관리자+사용권·대상 선택·역할 ID 입력이 필요하다. 배정 후 해당 역할이 조작 목록에 선택되며 명령은 별도로 접수한다.
6. 장비 제어의 역할·지원 기능·값을 선택하고 일반 명령을 접수한다. 요청값과 가상 상태를 구분해 확인한다.
   가상 상태의 시간은 가상 값 조회/반영 시각이며 실제 장비 관측이 아니다.
7. 기존 장비 변경은 장비 제어에서 **설정 편집에 불러오기** 후 저장한다.
   충돌하는 설정 변경은 기존 snapshot을 바꾸지 않고 이후 전송을 중단시킨다.
   모델·대상 설정을 저장하면 화면의 기존 상태를 새 대상의 관측으로 표시하지 않도록 재조회가 필요해진다.

가상 모델은 조광 조명/전원 전용 조명, 프로젝터, 음향, 승강을 제공한다.
장애 선택: None(정상), Failure(실패), Disconnected(끊김), NoResponse(무응답), ResponseLost(값 반영 후 응답 유실).
가상 지연은 0~30000ms다. 불확실 결과에는 자동 재시도가 없다.
역할 교체 시 저장된 시나리오가 요구하는 기능을 만족해야 한다.
STOP은 현재 사용권자의 지원 장비에만 허용하며 관련 시나리오의 후속 단계를 차단한다.

앱에는 주소·공개 지문·앱 PC ID만 `%LOCALAPPDATA%/IntegratedContro/client.json`에 저장한다.
비밀번호·인증 토큰은 저장하지 않는다. 테스트의 `--profile-dir`은 격리된 클라이언트 설정 폴더를 지정한다.
비밀번호 원문·토큰은 로그/작업 snapshot에 기록하지 않는다.

### 이전 배포본

App-role-fix는 역할 배정 수정까지, Lighting은 초기 조명 카드까지 포함한 이전 배포본이다.
현재 화면은 문서 첫 절의 최신 앱 바로가기와 최신 ControlHost를 사용한다. 이전 폴더는 최신 3개 보관 정책에 따라 정리한다.

## 시나리오·교대·선택 취소

2026-09-14 F2·F3·F4 수정 이후 소스 기준 (운영 적용 시 App·ControlHost 함께 업데이트):

- 장비 이름이나 PC 표시 이름만 바꿔도 접수된 명령·시나리오는 계속 처리되며 기존 가상 상태를 유지한다.
  대상 PC ID·연결·모델 등 실행 설정 변경은 재조회가 필요하다. 변경 전 늦은 응답은 원 작업 이력에서 확인하며,
  새 대상의 상태로 표시하지 않는다.
- 상단 **이전 사용자 작업 N건**에는 Hiperwall 대기·전송 중·불확실 작업도 포함된다.
  로그인·교대·정기 상태 갱신 시 자동으로 반영되며 같은 계정의 이전 세션도 구분한다.
- **작업 · 교대 → Hiperwall 진행·결과 확인 필요**에서 원 요청자·세션·Controller·대상·결과를 확인한다.
  현재 사용권과 전체 장비 제어 권한이 있으면 **선택 Hiperwall 미전송 부분 취소**로 남은 단계를 취소한다.
  이미 전송된 명령은 취소되지 않으며 불확실 결과는 Controller 목록과 대조한다.
  미전송 단계가 없으면 취소 버튼이 비활성화되고, Unknown 기록은 최근 100건을 넘어도 계속 표시된다.

장비 제어 탭의 역할·기능·값·대기·제한시간을 선택하고,
시나리오 탭에서 실패 정책과 선택적 가상 상태 확인 조건을 지정해 마지막 단계로 추가한다.
조건은 예를 들어 `Power`와 `1`이며 실행 시 최신 가상 상태를 한 번 조회해 비교한다.
조건 충족까지의 반복 대기는 이번 범위에 포함하지 않는다. 필요한 순차 대기는 단계별 대기로 설정한다.
정의 이름을 정해 저장하고 저장된 시나리오를 선택해 실행한다.

- 시나리오 시작 시 모든 단계의 역할·장비·PC·설정 버전·동작을 고정하고 장비 ID 단위로 예약한다.
- 사용 종료와 정상 앱 종료는 접수된 명령·시나리오·예약을 유지한다.
- 다음 사용권자는 작업 탭에서 원 요청자·원 세션·대상·진행·결과를 확인하고 선택 취소한다.
- 일반 작업 취소는 미전송 부분만 취소한다. 이미 전송된 결과는 보존한다. 물리 정지나 롤백이 아니다.
- 예약 충돌 시 수동 명령은 접수하지 않는다. 작업 탭에서 해당 시나리오를 선택해
  **시나리오 중단 후 수동 전환**을 명시적으로 승인한다.
- 시나리오가 실행 중이면 전송 결과를 기다린다. 중단 후 대상별 **가상 상태 조회·대조**를 수행한다.
  조회/제약을 확인하기 전에는 제한이 유지된다. 전환 완료 뒤 사용자가 새 명령을 선택한다.
- 전환 안내를 취소하면 원 시나리오는 계속되며 충돌 명령은 접수되지 않는다.
- ResponseLost로 불확실해진 가상 장비는 실제 저장된 가상 값을 조회해 대조할 수 있다.
  과거 명령 이력을 성공으로 덮어쓰거나 원 명령을 재실행하지 않는다.
- 전송 실패의 Continue 정책은 일반 실패에만 적용한다. 권한/설정/현장 불일치·만료·불확실 결과·취소에는 적용하지 않는다.

수동 명령 기본 만료는 24시간이며 서비스 요청 계약에서 1초~7일로 지정할 수 있다.
단계 대기는 0~1시간, 제한시간은 100ms~30초, 시나리오는 1~100단계다.
현재는 단일 실행 Worker가 공유 연결을 포함한 모든 전송을 직렬 처리한다.
대기 중인 시나리오는 비예약 장비 명령의 접수를 막지 않는다.

## 연결 이상·재시작 복구

정상 교대와 장애 인계를 구분한다.

1. 앱의 연결 이상 표시를 확인한다. 생존 확인은 앱에서 1초 간격이고 호스트 기본 제한은 15초다.
   `host.json` 또는 setup의 `--heartbeat-timeout`으로 3~300초를 설정한다(변경 후 호스트 재시작).
2. 호스트는 이전 세션을 차단하고 사용권 세대를 증가시킨다. 늦은 생존 확인으로 되살리지 않는다.
3. 관리자가 복구 탭에서 **진행 작업 확인**을 눌러 작업·예약·불확실 장비를 검토한다.
4. **관리자 복구 인계 승인** 후 다음 운영자가 사용 시작한다.
   확인 이후 작업 상태가 달라지면 진행 작업을 다시 확인해야 한다. 검토 표시는 5분 동안 유효하다.

차단된 이전 세션은 다시 로그인해야 한다. 호스트 재시작 시 모든 이전 인증 토큰은 무효다.
인증 세션은 최대 12시간이며 자동 로그인/자동 사용권 탈취는 제공하지 않는다.
로그아웃이 저장되면 해당 인증 토큰을 즉시 폐기한다. 다시 조회·복구 작업을 하려면 재로그인해야 한다.
단순 사용 종료는 인증 세션을 유지하며, 로그아웃도 이미 접수된 작업이나 다른 앱의 로그인 세션을 취소하지 않는다.
비정상 종료나 호스트 연결 불가 상태에서 로컬 앱이 사용권을 발급하지 않는다.

모든 앱을 닫아도 호스트 프로세스가 실행 중이면 접수 작업은 계속 처리된다.
호스트를 중지하려면 호스트 콘솔에서 Ctrl+C를 사용한다. Windows 로그아웃·재부팅은 별도 EXE도 종료한다.
다음 실행은 반드시 기존 폴더에 대해 `run --data`로 한다.

호스트 재시작 시:

- 전송 중 상태는 결과 불확실로 복구하고 자동 재전송하지 않는다.
- 중단한 시나리오는 대기 중/첫 단계 전이었더라도 자동 재개하지 않는다.
- 전송 전 영속 접수된 일반 명령만 현재 권한·설정·만료를 다시 확인해 실행할 수 있다.
- 취소 작업, 원 요청자·취소자·감사 기록은 유지한다.
- 불확실 장비 제한은 관리자 사용권 인계만으로 해제하지 않는다. 대상별 상태 대조가 별도로 필요하다.

## 보관·이전·현재 한계

호스트를 정상 중지한 뒤 선택 데이터 폴더 전체를 보관한다. 실행 중인 SQLite 파일 하나만 복사하지 않는다.
데이터 폴더 복사만으로 다른 Windows 사용자/PC에서 DPAPI 복호화가 보장되지 않는다.
계정/호스트 이전, 인증서 갱신(초기 자체 인증서 유효기간 2년), 비밀번호 분실 복구는
기존 현장 ID·계정·작업 이력을 보존하는 후속 운영 도구가 필요하다. 새 setup으로 문제를 덮지 않는다.

첫 저장 구현은 단일 SQLite 트랜잭션으로 운영 상태 aggregate와 감사 이력을 보존한다.
버전 1 외 DB는 진단 후 중지하며 자동 초기화하지 않는다.
대규모 기록 분할·페이지 조회·완료 이력 보존 정책·온라인 백업·설치/업데이트 패키지는 후속 항목이다.
활성/불확실 작업을 삭제하는 청소 기능은 넣지 않았다.

## 실제 서로 다른 두 PC에서 확인할 절차

다음은 수행할 절차이며 현재 완료 기록이 아니다.

1. 두 PC 각각의 에디션·25H2 빌드·x64·표시 배율과 호스트 PC/실행 계정을 기록한다.
2. 호스트에 로컬 폴더를 선택해 setup/run하고, LAN 주소/포트/지문으로 A와 B가 로그인한다.
3. 같은 계정의 두 세션, 서로 다른 운영자 계정 각각에서 사용 시작을 동시에 눌러 한 세션만 얻는지 확인한다.
   조회 세션의 일반 명령·취소·공유 설정이 차단되어야 한다.
4. 관리자 설정으로 가상 장비 두 개와 역할/별칭을 만든다. 미래 단계의 장비까지 사용하는 대기 포함 시나리오를 저장한다.
5. A가 일반 명령과 시나리오를 접수한다. 역할/설정 변경 후 기존 snapshot 불변과 전송 차단을 확인한다.
6. 예약 장비 충돌, 전환 안내 취소, 명시적 중단, 상태 대조, 새로운 수동 명령 순서를 확인한다.
   비예약 장비 명령은 접수되어야 한다.
7. 별도 실행에서 A가 지연 작업 두 개와 시나리오를 남기고 사용 종료한다.
   B가 남은 작업·원 요청자·단계·예약을 확인하고 하나만 취소한다. 다른 작업은 계속되어야 한다.
8. 두 앱 모두 정상 종료한다. 호스트 EXE가 작업을 완료하는지 확인하고 앱을 다시 열어 이력을 확인한다.
9. ResponseLost를 주입하고 전송 중 호스트를 종료·동일 폴더로 재시작한다.
   불확실 명령/시나리오가 재실행되지 않고 설정·계정·작업·감사 기록이 유지되어야 한다.
10. 제어 PC 연결을 끊거나 앱을 강제 종료한다. B에서 연결 이상 → 이전 세션 차단 → 관리자 작업 확인 → 복구 인계를 수행한다.
    연결을 복구한 A의 오래된 명령이 거부되는지 확인한다.
11. Pro와 Enterprise에서 배포 폴더 실행, 실제 네트워크 지연/손실, 방화벽, 로그인/로그아웃, PC 재부팅,
    100/150/200% 배율과 장시간 기록 증가를 별도로 확인한다.

확인한 PC별 증거와 실패/미검증 항목을 [검증 기록](docs/FIRST_IMPLEMENTATION_VALIDATION.md)에 추가한다.
로컬 두 클라이언트/두 창/두 EXE 시작 검증을 이 절차 완료로 기록하지 않는다.
