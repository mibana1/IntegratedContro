# IntegratedContro 작업 지침

## 먼저 읽을 문서

실제 작업을 시작할 때 [docs/PROJECT_GUIDE.md](docs/PROJECT_GUIDE.md)를 먼저 읽는다.
이 문서에 통합 범위, C# WPF 구조, 예상 화면, 필수 동작, 구현 단계, 미결정 사항과 원본 자료 위치가 있다.
사용자의 최신 명시적 지시가 저장된 문서보다 우선한다.

## 작업 원칙

- 이 폴더가 실제 Git 저장소다. 새 앱 소스는 이 저장소에 작성한다.
- 작업 공간 ../archive/legacy-reference는 이전 프로젝트의 참고 자료다. 불완전한 Node 배포본을 실행 대상으로 삼지 않는다.
- C# + WPF + XAML/MVVM과 SQLite 방향을 유지한다. 기존 JS/HTML/Node는 동작·테스트 참고용이다.
- 초기 공식 지원 OS는 Windows 11 25H2 Pro·Enterprise / x64다. 다른 OS는 필요 시 검증 후 추가한다. .NET 10을 사용하며 개발 PC의 SDK 10.0.400 설치를 확인했다.
- Core/Application에 WPF·Win32·COM·레지스트리 직접 의존을 두지 않는다. 장치·영상·보호 저장·OS 기능 탐지·설치 의존은 전용 어댑터와 진입점에 격리하고, 지원 판정과 어댑터 선택을 한곳에서 관리한다. 상세 경계는 PROJECT_GUIDE.md 5절을 따른다.
- UI·역할·장비 기능·드라이버·연결을 분리하며 장비 모델이나 주소를 화면에 고정하지 않는다.
- 가상 장비 상태와 실제 장비 관측, 저장과 LIVE, 전송과 물리 동작 완료를 구분한다.
- 일반 명령·시나리오는 접수/시작 시 대상과 설정을 snapshot으로 고정한다. 신규 접수 시 계정 권한·사용권을 검사하고, 접수 작업 전송 시 호스트 실행 권한·대상·작업 유효성을 다시 검사한다. 사용권 반납만으로 접수 작업을 취소하지 않는다.
- 여러 PC에서 앱 자체 계정으로 사용한다. 공통 실행 호스트가 계정·사용권·접수 작업과 명령 실행을 관리하며, 신규 제어는 권한 있는 한 사용자의 한 앱 세션만 요청한다.
- 초기 호스트는 UI와 별도 EXE로 실행하며 Windows 서비스로 전환할 수 있게 실행 수명과 공통 로직을 분리한다. UI 종료로 호스트를 중지하지 않는다.
- 호스트 운영 데이터는 최초 설정에서 지정한 로컬 폴더에 둔다. 실제 경로를 임의로 고정하거나 접근 실패 시 다른 폴더의 빈 DB로 대체하지 않는다. 서비스 전환 시 동일 데이터·계정·작업 이력을 유지하고 보호 저장 접근을 재검증한다.
- 초기 계정 정책은 호스트 최초 설정에서 관리자 생성 후 관리자가 운영자를 등록하는 방식이다. 추후 변경 가능하므로 등록 절차를 분리하고 기존 계정 ID·작업/감사 이력을 보존한다.
- PC 장치는 항상 PC ID·장치 ID로 대상을 확정하고 화면에 실제 PC 이름을 표시한다. 대상 미설정·연결 실패 시 UI/호스트 PC로 자동 대체하지 않는다.
- 사용 종료 후에도 접수된 명령·시나리오·표시/정리 작업은 원 요청자와 함께 유지한다. 다음 사용권자가 이전 사용자 작업을 확인하고 선택 취소할 수 있다. 사용 종료와 작업 취소를 구분한다.
- 연결 이상은 표시 → 이전 세션 신규 명령 차단 확인 → 진행 중 작업 확인 → 관리자 복구 인계 순서로 처리한다. 불확실한 명령을 자동 재전송하지 않는다.
- 시나리오가 사용하는 장비는 실행 동안 예약한다. 충돌하는 수동 조작은 현재 사용권자의 명시적인 '시나리오 중단 후 수동 전환'으로 처리하고, 전환 완료 뒤 새 조작만 실행한다. 중단한 시나리오를 자동 재개하지 않으며 STOP 예외는 PROJECT_GUIDE.md를 따른다.
- 원본 코드와 기준 문서의 차이는 근거를 확인해 처리한다. 확인하지 않은 프로토콜·검증 결과를 추정하지 않는다.
- 화면 시안은 구성 제안이다. 예시 상태·영상·장비 수를 실제 설정이나 고정 요구사항으로 복제하지 않는다.
- 구현 요청의 범위에서 진행하고 관련 검증을 수행한다. 문서 정리 요청만으로 앱 구현이나 실장비 제어를 시작하지 않는다.
- 첫 구현 완료 범위는 PROJECT_GUIDE.md 10절의 여러 PC 가상 장비 운영·교대·이전 작업 보존/취소·복구 흐름을 따른다. 확정된 초기 OS·.NET 10과 의존 분리 기준을 적용한다.
- 주요 범위·설계 결정·진척이 바뀌면 PROJECT_GUIDE.md의 현재 요약을 갱신한다. 날짜별 상세 변경·검증·배포 기록은 docs/history/IMPLEMENTATION_HISTORY.md에 작성하고 docs/README.md의 문서 목록을 유지한다.
- 완료 보고는 구현 내용, 실행한 검증, 남은 항목을 구분한다. 비밀값을 문서·로그에 기록하지 않는다.
- 작업 공간 파일 정리에서는 삭제된 자료의 목록·경로·용량·삭제 이력을 파일이나 문서로 새로 남기지 않는다. 자료 안내는 현재 남아 있는 파일을 기준으로 유지한다.
- 사용자 배포 규칙(2026-09-15): 앱 수정·검증 후 scripts/publish.ps1로 배포하고, 저장소 상위 작업 공간의 IntegratedContro.lnk가 최신 App을 가리키게 갱신한다. EXE/DLL을 작업 공간에 복사하지 않는다.
- 배포 빌드는 artifacts/publish의 최신 3개만 보관한다. 별도 dotnet publish를 사용했다면 scripts/sync-published-app.ps1 -BuildPath <완성된 최신 빌드 절대 경로>로 바로가기·보관 규칙을 적용한다. 실행 중인 빌드·잠긴 파일·운영 데이터·재분석 지점은 자동 삭제하지 않고 보류 사유를 보고한다. 서버나 실행 중 앱을 임의로 종료하지 않는다.
